# Project Rules

## Show Detail Page Route
The show detail page route is `/showlist/show/{id}`, NOT `/show/{id}`.
All links to a show's detail page MUST use `/showlist/show/@id` format.
Never use the short `/show/@id` format — it will 404.
